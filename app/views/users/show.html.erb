<h2> Welcome to <%= @user.name %>'s profile page!</h2>

<p> <%= image_tag @user.profile.url(:thumb) %> <% @user.artworks.each do |artwork| %>
  <li><%= image_tag artwork.image.url(:thumb) %> <br/> <%= link_to artwork.title, artworks_path(artwork) %></li><br />
  <% end %> </p>

<% if @user == current_user %> 
	<% %>
<% else %>
	<p>Want to send <%= @user.name %> a message? <%= link_to "Click here", new_conversation_path(@user) %>.</p>
<% end %>

<p><%= link_to "See More Artists", users_path %></p>