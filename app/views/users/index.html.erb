<h1>All artists</h1>
<ul>
  <% @users.each do |user| %>
  <li> <%= link_to image_tag user.profile.url(:thumb) %> <br /> <%= link_to user.name, user_path(user) %> <br /> <%= link_to "Message me", new_conversation_path(@user) %> </li><br />
  <% end %>
</ul>

<%= link_to "Home", root_path %>
