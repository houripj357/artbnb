<h1>All artists</h1>
<ul>
  <% @users.each do |user| %>
  <li> <%= image_tag user.profile.url(:thumb) %> <br /> <%= link_to user.name, user_path(user) %> <br /> send this artist a message (I must add this functionality) </li><br />
  <% end %>
</ul>

<%= link_to "Home", root_path %>
