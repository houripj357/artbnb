<% if @venue.errors.any? %>
	<div class="row">
	  	<div class="col-xs-8 col-xs-offset-2">
	  		<div class="panel panel-danger">
	  			<div class="panel-heading">
	  				<h3 class="panel-title">
	  					<%= pluralize(@venue.errors.count, "error") %>
	  					prohibited this venue from being registered
	  				</h3>
	  				<div class="panel-body">
	  					<ul>
	  						<% @venue.errors.full_messages.each do |msg| %>
								<li><%= msg %></li>
							<% end %>
						</ul>
					</div>
				</div>
			</div>
		</div>
	</div>
<% end %>

<%= form_for @venue, html: { multipart: true } do |f| %>
	<p>
		<%= f.label :venue %><br/>
		<%= f.file_field :image %>
	</p>
	<p>
		<%= f.label :name %><br/>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :description %><br/>
		<%= f.text_area :description, rows: 5 %>
	</p>
	<p>
		<%= f.label :street %><br/>
		<%= f.text_area :street %>
	</p>
		<p>
		<%= f.label :street_cont %><br/>
		<%= f.text_area :street_cont %>
	</p>
		<p>
		<%= f.label :city %><br/>
		<%= f.text_area :city %>
	</p>
		<p>
		<%= f.label :state %><br/>
		<%= f.text_area :state %>
	</p>
		<p>
		<%= f.label :zip %><br/>
		<%= f.text_area :zip %>
	</p>
		<p>
		<%= f.label :country %><br/>
		<%= f.text_area :country %>
	</p>
	<p>
		<%= f.label :phone_number %><br/>
		<%= f.text_field :phone_number %>
	</p>	
	<p>
		<%= f.submit %>
	</p>
<% end %>