<h1>All artwork</h1>
<p>
<ul>
  <% @artworks.each do |artwork| %>
  <li>Title: <%= link_to artwork.title, artwork_path(artwork) %> | <%= artwork.description %></li>
  <% end %>
</ul>
</p>
<p>
	<%= link_to "Upload artwork", new_artwork_path %>
</p>
