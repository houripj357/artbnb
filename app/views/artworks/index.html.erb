<h1>All artwork</h1>
<p>
<ul>
  <% @artworks.each do |artwork| %>
  <li><%= image_tag artwork.image.url(:medium) %> <br/> <%= link_to artwork.title, art_path(artwork) %></li>
  <% end %>
</ul>
</p>
<p>
	<%= link_to "Upload artwork" %>
</p>
