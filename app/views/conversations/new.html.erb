<h1>Send new message</h1>

<%= form_tag conversations_path, method: :post, multipart: true do %>

<!-- <div id="friend-lookup">
    <%= form_tag search_friends_path, remote: true, method: :get do %>
    <div class="form-group row no-padding text-center col-md-12">
        <div class="col-md-6">
            <%= text_field_tag :search_param, params[:search_param],
            	placeholder: "First name or Last name", autofocus: true,
                class: 'form-control search-box input-lg'  %>
        </div>    

        <div class="col-md-2">
            <%= button_tag(type: :submit, class: "btn btn-warning btn-lg purple-button") do %>
                <i></i> Search
            <% end %>
        </div>
    </div>

    <% end %>
</div> -->


	<div class="form-group row no-padding col-md-12">
	<div class="col-md-8">
	<%= select_tag :user_id, options_from_collection_for_select(User.all - [@current_user], :id, :name) %>
	</div>
	</div>

	<div class="form-group row no-padding text-center col-md-12">

	<div class="col-md-8">
		<%= text_field_tag :subject, nil, placeholder: "Subject", class: 'form-control input-lg' %>
	</div>

	<div class="col-md-8">
		<%= text_area_tag :body, nil, placeholder: "Leave a message", class: 'form-control input-lg' %>
	</div>

	</div>

	<div>
		<%= file_field_tag :attachment %>
	</div>

	<br />

	<div>
		<%= button_tag(type: :submit, class: "btn btn-warning btn-lg purple-button") do %>
	                <i></i> Send Message	
	    <% end %>
	</div>
<% end %>
<hr />